<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Pagamento</title>
</head>
<body>
    <h1>Editar Pagamento</h1>
    <form action="/pagamentos/<%= pagamento.id %>?_method=PUT" method="POST">
        <label for="appointment_id">Appointment ID:</label>
        <input type="number" name="appointment_id" id="appointment_id" value="<%= pagamento.appointment_id %>" required>
        <br>
        <label for="amount">Amount:</label>
        <input type="number" step="0.01" name="amount" id="amount" value="<%= pagamento.amount %>" required>
        <br>
        <label for="payment_method">Payment Method:</label>
        <select name="payment_method" id="payment_method" required>
            <option value="credit_card" <%= pagamento.payment_method === 'credit_card' ? 'selected' : '' %>>Credit Card</option>
            <option value="debit_card" <%= pagamento.payment_method === 'debit_card' ? 'selected' : '' %>>Debit Card</option>
            <option value="cash" <%= pagamento.payment_method === 'cash' ? 'selected' : '' %>>Cash</option>
            <option value="online" <%= pagamento.payment_method === 'online' ? 'selected' : '' %>>Online</option>
        </select>
        <br>
        <label for="status">Status:</label>
        <select name="status" id="status" required>
            <option value="pending" <%= pagamento.status === 'pending' ? 'selected' : '' %>>Pending</option>
            <option value="completed" <%= pagamento.status === 'completed' ? 'selected' : '' %>>Completed</option>
            <option value="failed" <%= pagamento.status === 'failed' ? 'selected' : '' %>>Failed</option>
            <option value="refunded" <%= pagamento.status === 'refunded' ? 'selected' : '' %>>Refunded</option>
        </select>
        <br>
        <label for="transaction_date">Transaction Date:</label>
        <input type="datetime-local" name="transaction_date" id="transaction_date" value="<%= pagamento.transaction_date %>" required>
        <br>
        <button type="submit">Atualizar Pagamento</button>
    </form>
    <a href="/pagamentos/<%= pagamento.id %>">Voltar</a>
</body>
</html>
