<h1>Lista de Vendas</h1>

<!-- Formulário de busca -->
<form id="searchForm" method="GET" action="/vendas">
    <div class="form-group">
        <label for="search">Buscar Venda:</label>
        <input type="text" id="search" name="search" class="form-control" placeholder="Digite o ID do Usuário ou do Produto" autocomplete="off">
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>ID do Usuário</th>
            <th>ID do Produto</th>
            <th>Quantidade</th>
            <th>Valor Total</th>
            <th>Data da Venda</th>
            <th>Ação</th>
        </tr>
    </thead>
    <tbody id="vendaTableBody">
        <% vendas.forEach(venda => { %>
            <tr>
                <td><%= venda.id %></td>
                <td><%= venda.idUser %></td>
                <td><%= venda.idProduct %></td>
                <td><%= venda.quantidade %></td>
                <td><%= venda.valorTotal.toFixed(2) %></td>
                <td><%= venda.DataVenda.toISOString().split('T')[0] %></td>
                <td>
                    <a href="/vendas/<%= venda.id %>" class="btn btn-primary">View</a>
                    <a href="/vendas/<%= venda.id %>/edit" class="btn btn-warning">Edit</a>
                    <form action="/vendas/<%= venda.id %>?_method=DELETE" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>

<script>
    document.getElementById('search').addEventListener('input', function() {
        const searchValue = this.value;

        fetch(`/vendas/search?search=${encodeURIComponent(searchValue)}`)
            .then(response => response.json())
            .then(data => {
                const tbody = document.getElementById('vendaTableBody');
                tbody.innerHTML = ''; // Limpa as linhas existentes

                data.vendas.forEach(venda => {
                    const tr = document.createElement('tr');
                    tr.innerHTML = `
                        <td>${venda.id}</td>
                        <td>${venda.idUser}</td>
                        <td>${venda.idProduct}</td>
                        <td>${venda.quantidade}</td>
                        <td>${venda.valorTotal.toFixed(2)}</td>
                        <td>${venda.DataVenda.toISOString().split('T')[0]}</td>
                        <td>
                            <a href="/vendas/${venda.id}" class="btn btn-primary">View</a>
                            <a href="/vendas/${venda.id}/edit" class="btn btn-warning">Edit</a>
                            <form action="/vendas/${venda.id}?_method=DELETE" method="POST" style="display: inline;">
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        </td>
                    `;
                    tbody.appendChild(tr);
                });
            })
            .catch(error => console.error('Error:', error));
    });
</script>
